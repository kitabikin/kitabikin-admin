name: CI Dev

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create TEST file
        run: echo "TEST=${{ secrets.ENVIRONMENT }}"

  deploy:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - run: echo "Deploy ${{ github.ref }} to server"
